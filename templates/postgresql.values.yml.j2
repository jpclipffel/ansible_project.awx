postgresqlImage:
  tag: "10"

pgpoolImage:
  tag: "4"

global:
  postgresql:
    username: "{{ pg_username }}"
    password: "{{ pg_password }}"
    database: "{{ pg_database }}"

postgresql:
  replicaCount: 2

pgpool:
  useLoadBalancing: false

service:
  type: ClusterIP
